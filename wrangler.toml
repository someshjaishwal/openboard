# Pages CI deploys with:
#   npx wrangler pages deploy apps/web/dist --project-name=openboard-web
# `pages deploy` cannot use -c. `wrangler deploy` is Workers-only.
name = "openboard-web"
compatibility_date = "2026-09-01"
pages_build_output_dir = "apps/web/dist"
